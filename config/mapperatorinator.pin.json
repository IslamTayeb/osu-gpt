{
  "repo": "https://github.com/IslamTayeb/Mapperatorinator.git",
  "branch": "main",
  "sha": "4c0b573e",
  "upstreamBase": "2a70eb89",
  "notes": "Fork main is pristine OliBomby upstream plus exactly two additive commits: the osu-gpt batch driver, and the SDPA selection fix the fast decode path needs. To take a new upstream release, reset main to upstream and replay those two. Cluster identity (SSH host, account, paths) lives in dcc.local.json, untracked — see dcc.local.example.json."
}
